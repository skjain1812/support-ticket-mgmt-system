Integration tests for the Support Ticket Management System.

**Status:** Not yet implemented — planned for Phase 4 per `test-strategy.md`.

When added, this folder will contain:

- `package.json` with Jest + supertest
- `integration/` — state machine and API tests

Run from this folder:

```powershell
npm install
npm test
```
